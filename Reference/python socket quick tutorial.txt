TCP = socket.SOCK_STREAM
IPv4 = socket.AF_INET


*******Server usual routine******

if you want to get ip inside the program:

HOST = socket.gethostbyname(socket.gethostname())

server.socket(socket.AF_INET, socket.SOCK_STREAM)
server.bind((HOST, PORT))
server.listen(5)
conn, addr = s.accept

then:

recv(bytes).decode(decoding)
send(message).encoding(encoding)


*********Client usual routine********

socket.socket(...)
socket.connect((HOST, PORT))

then:

recv...
send...